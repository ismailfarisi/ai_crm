# ─── Docker / Postgres ────────────────────────────────────────────────────────
POSTGRES_USER=crm
POSTGRES_PASSWORD=crm_dev_password
POSTGRES_DB=crm
POSTGRES_PORT=5433
REDIS_PORT=6380

# Copy this file to `.env` at the repo root (used by docker compose),
# and to `apps/api/.env` (used by the NestJS app).
